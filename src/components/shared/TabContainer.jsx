import { styled } from "../../config/stitches.config";

const TabContainer = styled("div", {
  maxWidth: "800px",
  margin: "auto",
  paddingLeft: "30px",
  paddingRight: "30px",
});

export default TabContainer;
